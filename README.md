# Common Utilities

Shared Go utility functions used across all backend services.

## Packages
- `httputil` — HTTP middleware and helpers
- `logging` — Structured logging setup
- `config` — Environment config loader
